# CODEX.md

Codex adapter entrypoint.

1. Read `AGENTS.md` first for the shared workflow, source-of-truth map, commands, completion rule, and safety boundaries.
2. Read `.agents/adapters/codex/README.md` for Codex-specific notes.
3. Use `.project/` contracts and `.agents/` runtime assets unless a task explicitly says otherwise.

Keep this file thin. Shared rules belong in `AGENTS.md`; Codex-only notes belong in `.agents/adapters/codex/README.md`.
